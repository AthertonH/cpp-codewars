#include <iostream>
#include <string>
using namespace std;

bool feast(string beast, string dish){
    if(beast.front() == dish.front() && beast.back() == dish.back()){
        return true;
    } else {
        return false;
    }
}

int main()
{
    ("", "");
}